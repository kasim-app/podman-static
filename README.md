# podman-static

## Install

```bash
curl -fsSL https://github.com/kasim-app/podman-static/raw/main/install.sh | bash
```

## Uninstall
```bash
sudo rm -rf /opt/podman
sudo rm -f /usr/local/bin/{podman,conmon,crun}
```
